FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source (will be overridden by volume)
COPY . .

EXPOSE 3000

# Run Next.js in dev mode
CMD ["npm", "run", "dev"]
